/* 
GeoQuery

*/

jQuery.fn.map = function(options) {
  options = jQuery.extend({
     name: "GeoQuery",
     size: 5,
     global: true
  }, options);

  //var map = new OpenLayers.Map(this,options); 
  alert(this); 
}

